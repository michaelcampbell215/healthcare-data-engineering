# Authentication (Option 1 — default, uses gcloud ADC)
GCP_PROJECT_ID=your-gcp-project-id
GCS_BUCKET_NAME=your-bucket-name
CSV_FILE_PATH=path/to/your/general_payments.csv

# Authentication (Option 2 — service account key for CI/CD or production)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json
be